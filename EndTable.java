package for_inventory;

public class EndTable extends Table{
	
	// EndTable should have material and color instance variables, both Strings
	
	// Write three methods (use DiningTable as your model):
	
	// 1.  A constructor
	// 2.  A toString()
	// 3.  A void method that says if the EndTable can serve as a matching side table set:
	//         If the quantity is even, then this method prints "This end table can be matching side tables.".
	//         Otherwise, print "This end table cannot be matching side tables.".
	
}
